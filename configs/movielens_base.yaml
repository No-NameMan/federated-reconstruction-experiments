experiment:
  name: movielens_fedrecon_base
  seed: 42
  mode: quick
  output_dir: results/raw

data:
  dataset: movielens_1m
  data_dir: data
  min_ratings_per_user: 20
  user_split:
    train: 0.8
    val: 0.1
    test: 0.1
  support_fraction: 0.5
  split_seed: 42

model:
  embedding_dim: 50
  use_user_bias: true
  use_item_bias: true
  init_std: 0.05

federated:
  rounds: 100
  clients_per_round: 50
  client_dropout_prob: 0.0

reconstruction:
  steps: 5
  lr: 0.1
  optimizer: sgd

client_update:
  steps: 1
  lr: 0.05
  optimizer: sgd

server:
  optimizer: sgd
  lr: 1.0

compression:
  enabled: false
  method: none

evaluation:
  eval_every: 5
  max_eval_clients: 500

logging:
  checkpoint_every: 25
